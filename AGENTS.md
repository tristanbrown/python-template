# AGENTS.md

See `.agents/AGENTS.GLOBAL.md` for full global instructions.  
Then read `AGENTS.PROJECT.md` for project-specific overrides.
Then read `AGENTS.LOCAL.md` for workspace-specific overrides.
