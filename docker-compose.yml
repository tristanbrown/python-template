### docker-compose.yml ###
## Access the jupyterlab instance at:
## https://${HOSTNAME}:8081/${PROJECTSLUG}/lab

version: "3.7"

services:
  python_template:
    extends:
      file: ${JUPYTER_SERVICE}
      service: jupyter

networks:
  caddy:
    external: true
